# ربط مكتبة الكتب (Google Drive) بالنظام

## ليه بدنا "حساب خدمة" (Service Account)؟
عشان السيرفر (اللي شغّال 24 ساعة بدون حدا يفتحله المتصفح) يقدر يقرأ الكتب
من Google Drive تبعك أوتوماتيك، بدون ما يطلب منك تسجيل دخول كل مرة.

## الخطوات

1. روح لـ **console.cloud.google.com** وسجّل دخول بنفس حساب Google يلي فيه
   مكتبة الكتب (NABIL_AI_BOOK_LIBRARY)

2. فوق يسار، دوس عالقائمة المنسدلة جنب "Google Cloud" ودوس **New Project**
   - اسم المشروع: `NabilAI` ثم **Create**

3. بمربع البحث فوق، اكتب **Google Drive API** ودوس عليه، ثم دوس **Enable**

4. من القائمة الجانبية (☰) روح لـ **APIs & Services** → **Credentials**

5. دوس **+ Create Credentials** → اختار **Service Account**
   - الاسم: `nabilai-drive-reader` → **Create and Continue** → **Done**
   (تجاهل الخطوات الاختيارية، دوس Done مباشرة)

6. رح تشوف الحساب الجديد بالقائمة - دوس عليه، بعدين تبويب **Keys**

7. دوس **Add Key** → **Create new key** → اختار **JSON** → **Create**
   - رح ينزّل ملف JSON صغير لجهازك تلقائياً - **هاد الملف مهم جداً وسري،
     ما ترسله لحدا**

8. افتح الملف يلي نزل (بأي برنامج نصوص زي Notepad)، وانسخ منّو سطر
   `"client_email"` - رح يكون شكله متل:
   `nabilai-drive-reader@nabilai-xxxxx.iam.gserviceaccount.com`

9. روح لـ Google Drive تبعك، افتح مجلد **NABIL_AI_BOOK_LIBRARY**، دوس
   كبسة يمين → **Share** → الصق الإيميل يلي نسخته بالخطوة 8 → صلاحية
   **Viewer** (قارئ فقط) → **Send**

## رفع الملف السري على Railway (بدون ما ينكشف لحدا)

1. افتح ملف الـ JSON يلي نزّلته، انسخ **كل محتواه** (Ctrl+A ثم Ctrl+C)
2. روح لمشروعك بـ Railway → خدمة **web** → تبويب **Variables** → **New Variable**
3. الاسم: `GOOGLE_DRIVE_CREDENTIALS_JSON`
4. القيمة: الصق كل محتوى الملف
5. دوس **Deploy** لتفعيل التحديث

بهيك السيرفر بيقدر يقرأ الكتب أوتوماتيك، والملف السري محفوظ بأمان بإعدادات
Railway وما حد شايفه غيرك.

---

**ملاحظة:** بالمرحلة الجاية رح نضيف كود صغير يقرأ هالمتغير ويحوّله لملف
`drive_service_account.json` وقت التشغيل (بدل رفع ملف فعلي عالـ GitHub،
لأنو GitHub عام والملف سري).
