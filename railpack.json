{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "buildAptPackages": ["gcc", "g++"],
  "deploy": {
    "aptPackages": ["libstdc++6", "libgomp1", "tesseract-ocr"],
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
