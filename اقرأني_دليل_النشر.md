# دليل رفع ونشر NabilAI (بدون أي خبرة تقنية)

## المرحلة 1: رفع الكود على GitHub (5 دقائق)

1. افتح المستودع تبعك: `github.com/nabilakil1974-ship-it/nabil-ai-school`
2. دوس على زر **Add file** (فوق يمين) ثم اختر **Upload files**
3. فك ضغط المجلد `nabil-ai-backend` يلي حعطيك ياه، وقم بسحب **كل الملفات والمجلدات
   يلي جوّاه** (مش المجلد نفسه) وإفلاتها بصفحة GitHub
4. تحت بالصفحة اكتب أي رسالة زي "رفع النسخة الأولى" ودوس **Commit changes**

## المرحلة 2: ربط المشروع بـ Railway (10 دقائق)

1. روح لموقع railway.app وسجّل دخول بحساب GitHub تبعك (نفس الحساب)
2. دوس **New Project** ثم **Deploy from GitHub repo** واختر `nabil-ai-school`
3. Railway حيبلش يبني المشروع أوتوماتيك (ممكن ياخد دقيقة لدقيقتين)

## المرحلة 3: إضافة قاعدة البيانات (دقيقتين)

1. جوّا نفس المشروع بـ Railway، دوس **New** ثم **Database** ثم **PostgreSQL**
2. Railway بينشئ قاعدة البيانات ويربطها أوتوماتيك - رح نستخدم قيمتها بالخطوة الجاية

## المرحلة 4: ضبط الإعدادات السرية (5 دقائق)

بمشروع Railway، روح لتبويب **Variables** وضيف هالقيم:

| الاسم | القيمة |
|---|---|
| `OPENAI_API_KEY` | مفتاح OpenAI تبعك (من platform.openai.com) |
| `DATABASE_URL` | Railway بيعبّيها إلك أوتوماتيك لما تربط الـ PostgreSQL - إذا ما ظهرت، دوس على قاعدة البيانات وانسخ "Postgres Connection URL" |
| `SECRET_KEY` | أي جملة عشوائية طويلة (مثلاً: `nabilai-secret-2026-xyz`) |

## المرحلة 5: التأكد إنو الموقع شغّال

1. بتبويب **Settings** بمشروع Railway، رح تلاقي رابط عام (Public Domain) - دوس
   عليه لتفعيله إذا مش مفعّل
2. افتح الرابط + `/api/health` بالمتصفح - إذا شفت `{"status":"ok"}` معناتها كل شي تمام ✅

---

**هيك بيكون عندك backend شغّال وحي على الإنترنت، وكل مرة ترفع تحديث جديد
عالـ GitHub (بنفس طريقة المرحلة 1)، Railway بينشره أوتوماتيك بدون ما تعمل
شي إضافي.**

بالمرحلة الجاية رح نبني: نظام أرشفة وبحث الكتب (RAG) عشان الأستاذ نبيل
يقدر يرجع لصفحات الكتب الحقيقية وهو عم يشرح.
